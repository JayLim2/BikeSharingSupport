import { AdminDataPipe } from './admin-data.pipe';

describe('AdminDataPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminDataPipe();
    expect(pipe).toBeTruthy();
  });
});
